{
  "name": "api",
  "version": "1.0.0",
  "main": "server.js",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "mongoose": "^5.12.4",
    "typescript": "^4.2.4"
  },
  "scripts": {
    "server": "ts-node-dev --inspect --clear --respawn --transpile-only --ignore-watch node_modules src/server.ts",
    "serv": "ts-node-dev --clear --respawn --transpile-only --ignore-watch node_modules src/server.ts"
  },
  "devDependencies": {
    "@types/express": "^4.17.11",
    "ts-node-dev": "^1.1.6"
  }
}